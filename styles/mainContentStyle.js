const styles = theme => ({
  markdown: {
    padding: `${theme.spacing.unit * 3}px 0`,
  },
  mainGrid: {
    marginTop: theme.spacing.unit * 3,
  },
  listRoot:{
    width: '100%',
  },
})

export default styles;
